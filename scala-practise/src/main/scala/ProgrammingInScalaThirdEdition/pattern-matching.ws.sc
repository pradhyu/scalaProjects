
// simple pattern matching with case classes
// case classes are scala's way of doing pattern matching.
// sealed case classes make sure the class is sealed and can't be extended outside the file sealed super class is defined


for (i <- 1 until 3;
     j <- 100 until 300 by 100
) yield i * j







